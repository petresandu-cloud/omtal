**What this changes**, in one paragraph.

**How it was verified.** The exact commands run and what they printed. "Should work" is not verification.

**If it touches a check or a rule:** which rule page it rests on, and what input now gives a different verdict than before, and why that is right.

**If it touches the page:** REPORT-PRINCIPLES.md updated in the same change, and `tests/test_report.py` passes.

- [ ] `python3 -m unittest` passes
- [ ] `python3 -m omtal self-test` passes
- [ ] No store policy text added to the repository
